{
  "name": "heic2jpg",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "bin": {
    "heic2jpg": "./dist/app.js"
  },
  "scripts": {
    "build": "tsc && node -e \"const fs=require('fs'); const now=new Date(); const timestamp=now.toISOString().split('T')[0] + ' ' + now.toTimeString().split(' ')[0]; fs.writeFileSync('dist/build-info.json', JSON.stringify({timestamp}))\"",
    "build:watch": "tsc -w",
    "start": "node dist/app.js",
    "dev": "ts-node src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "cli-progress": "^3.12.0",
    "commander": "^14.0.3",
    "exifr": "^7.1.3",
    "heic-convert": "^2.1.0",
    "picocolors": "^1.1.1",
    "piexifjs": "^1.0.6"
  },
  "devDependencies": {
    "@types/cli-progress": "^3.11.6",
    "@types/commander": "^2.12.0",
    "@types/node": "^25.6.0",
    "@types/piexifjs": "^1.0.0",
    "esbuild": "^0.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  }
}
